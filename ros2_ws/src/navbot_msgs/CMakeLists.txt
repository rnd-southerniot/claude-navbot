cmake_minimum_required(VERSION 3.8)
project(navbot_msgs)

find_package(ament_cmake REQUIRED)

# TODO: Add message generation here only if standard ROS messages stop being
# sufficient for controller diagnostics or other navbot-specific interfaces.

ament_package()
