# navbot_utils

Reserved package namespace for future Navbot helper utilities.

## Current state

Placeholder. Contains a `scripts/` directory but no scripts and no
`package.xml`. Does **not** build. It is a namespace reservation
only; do not import it.

When small helper tools are needed (e.g. bag inspection, offline
calibration helpers), they can live here rather than scattering
across feature packages.

## See also

- [../../README.md](../../README.md) — workspace-level package list.
